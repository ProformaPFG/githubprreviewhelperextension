{
  "name": "github-code-review-assistant",
  "version": "1.2.0",
  "description": "Automated code analysis for GitHub PRs. Detects security and quality issues.",
  "type": "module",
  "scripts": {
    "build": "npm run clean && npm run compile && npm run bundle-content && npm run copy-assets",
    "build:action": "node action/build.js",
    "bundle-content": "node scripts/bundle-content.js",
    "compile": "tsc",
    "copy-assets": "node scripts/copy-assets.js",
    "clean": "rimraf dist",
    "watch": "tsc --watch",
    "package": "npm run build && cd dist && zip -r ../github-code-review-assistant.zip ."
  },
  "dependencies": {
    "@actions/core": "^2.0.0",
    "@actions/github": "^8.0.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "esbuild": "^0.27.3",
    "typescript": "^5.3.0",
    "rimraf": "^6.1.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
